{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}
    <h1>Profil</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ profil.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ profil.username }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ profil.image }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ profil.age }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ profil.role }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_profil_index') }}">back to list</a>

    <a href="{{ path('app_profil_edit', {'id': profil.id}) }}">Modifier votre profil</a>

    {{ include('profil/_delete_form.html.twig') }}
{% endblock %}
